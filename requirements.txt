# ============================================================
# 环境学子雷达 - 运行依赖
# ============================================================
# 版本固定为 2026-09-18 的 PyPI 最新稳定版。
# 之所以固定：原先 workflow 里是 `pip install <包名>`（不带版本），
# CI 每天都会装当天的最新版——上游任何一次破坏性变更都可能让第二天的
# 定时任务直接失败，而失败原因与本次代码改动毫无关系，极难排查。
# 固定后行为与固定当天完全一致，且升级变成一次显式决策。
#
# 升级方式：pip install -U <包名>，确认脚本跑通后改下面的版本号。
# 本地安装：pip install -r requirements.txt

feedparser==6.0.14
PyYAML==6.0.3
jieba==0.42.1
readability-lxml==0.9
html2text==2025.4.15
requests==2.34.2
trafilatura==2.2.0
